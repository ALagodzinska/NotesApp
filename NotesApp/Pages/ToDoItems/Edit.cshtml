@page
@model NotesApp.Pages.ToDoItems.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ToDoItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-row align-items-center">
                <input type="hidden" asp-for="ToDoItem.Id" />
                <input type="hidden" asp-for="ToDoItem.CreationDate" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                @*<div class="form-group">
                <input asp-for="ToDoItem.Content" class="form-control mb-2" />
                <span asp-validation-for="ToDoItem.Content" class="text-danger"></span>
                </div>
                <div class="form-group">
                <div class="form-check mb-2">
                <input class="form-check-input" asp-for="ToDoItem.IsDone" id="autoSizingCheck" />
                </div>
                </div>*@

                <div class="input-group">
                    <div class="input-group-prepend">
                        <input class="form-check-input" asp-for="ToDoItem.IsDone" id="CheckBoxForToDO"/>
                    </div>
                    <input asp-for="ToDoItem.Content" class="form-control mb-2" />
                    <span asp-validation-for="ToDoItem.Content" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index" asp-route-noteId="@Model.ToDoItem.NoteId">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
